import React from "react"

const Pcture = props => {
    return (
        <div className="detail-picture">
            <img src={props.picture} alt=""></img>
        </div>
    );
}

export default Pcture;